num = [3, 3, 9, 30, 8, 10, 23, 938, 99]


def sum_of_numbers(number):
    total = 0
    for i in number:
        total += i
    return total


print(sum_of_numbers(num))

# Calculate the sum of numbers within an array.
